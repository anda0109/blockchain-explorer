#ifndef __H_UT_STL__
#define __H_UT_STL__
#include <string>
#include <vector>
#include <map>
using namespace std;

//将一个string按指定的分隔符分离成多个字符串
vector<string> split(const string &s, const string &seperator);

#endif // !__H_UT_STL__
